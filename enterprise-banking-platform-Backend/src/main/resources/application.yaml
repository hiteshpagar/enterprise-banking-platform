spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/enterprise_banking_platform
    username: postgres
    password: Plainsurf1810

  mail:
    host: ${SMTP_HOST}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME}
    password: ${SMTP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        format_sql: true

    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

app:
  frontend-url: http://localhost:8081

jwt:
  secret: 7vN3LqK9xR2mFp8YsHd4UaW6ZjBcE1NtG5PvRwTxCyJuKsQa
  expiration: 86400000
